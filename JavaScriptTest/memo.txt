https://github.com/tetsu-ur/Zackmest.git

・グラフの初期値は軸だけにするか、無描画にするか？
・canvas の幅を動的に変更させる
・グラフ描画するIDを引数で指定する

・背景をグラデーションさせると格好いいかもしれない
・期限線の▼の幅がグリッド幅になっているのを固定にする
・１グリッドが２人月以上の場合に、１グリッドを分割して表示する
・グラフ描画対象のスライダのキャプション背景色をアクティブ的な色にする
・テストデータを計算対象の見積りごとに配列で保持させる
・グラフタイトルをどうするか検討する
・ブラフの拡大縮小を検討する


・凡例をつける[完了]
・工程ごとのキャプションをつける[完了]

