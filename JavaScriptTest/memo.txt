https://github.com/tetsu-ur/Zackmest.git

■フロントエンド

・ページ内に説明文などを記載する
・「はじめに」「使い方」「技術紹介」ページを作成する
・基礎数値セクションの表示を見やすくする
・グラフの初期値は軸だけにするか、無描画にするか？
・canvas の幅を動的に変更させる
・グラフ描画するIDを引数で指定する

・背景をグラデーションさせると格好いいかもしれない
・期限線の▼の幅がグリッド幅になっているのを固定にする
・１グリッドが２人月以上の場合に、１グリッドを分割して表示する

・タイトル画像を作成する[完了]
・ちゃんとしたサイトスタイルにする[完了]
・背景を多重スクロールにする[完了]
・数値のテキストフィールドをスピナにする[完了]
・多重スクロールで初回スクロール時に背景が横にビクッとずれる[完了]
・ブラフの拡大縮小を検討する [完了]
・工程・工数テーブルが初期時にアクティブな色にならない [完了]
・グラフの大きさを縮める　[完了]
・スライダで選択された工程・工数テーブルの要素の背景色をアクティブ的な色にする[完了]
・グラフタイトルをどうするか検討する[完了]
・凡例とスライドキャプションが意味的にかぶっているので統一したい[完了]
・グラフ描画対象のスライダのキャプション背景色をアクティブ的な色にする[完了]
・見積結果表示欄を作成する[完了]
・条件指定欄を追加[完了]
・凡例をつける[完了]
・工程ごとのキャプションをつける[完了]
・テストデータを計算対象の見積りごとに配列で保持させる[完了]


■サーバ
・ファンクションポイントとプログラムサイズが両方ある場合にどうするか
・計算方法を画面で確認できるようにする
・比較に使用する単メトリクスを変更できるようにする
・ログに出力する
・新規／改修を選択できるようにする
・単メトリクス見積りの方式を変える
	→生産性を使わない
・工程範囲パターンごとに標準工期、最短開発期間を算出する
・工程ごとの工数を固定にするか選択できるようにする

・入力チェック処理を修正する [完了]
・生産性入力値を初期表示する [完了]

■Tips
　○Eclipse のエディタで編集した内容がサーバに反映されない
	→普段は反映されるがたまに反映されないことがある。
		「Project」-> clean を行う。
		「Servers」ビューでサーバを選択してコンテキストメニュー -> Clean
		「Servers」ビューでサーバを選択してコンテキストメニュー -> Publish
		「Servers」ビューでサーバのリソース(Web AP)を選択してコンテキストメニュー -> Clean Module Work Directory
	→それでもだめならサーバ内のファイルと同じ内容のファイルがどこかに残っていることが考えられるので、
	  Navigator から削除して Clean しなおす。

